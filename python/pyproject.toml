[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pytest-substrate"
version = "0.1.0"
description = "pytest plugin for the substrate AWS emulator"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
authors = [
    {name = "Scott Friedman"},
]
dependencies = ["pytest>=9.0.3"]

[project.optional-dependencies]
# boto3 is not needed to use the plugin; it is needed to *test* substrate through the
# wire protocol botocore chooses, which is not the one aws-sdk-go-v2 chooses. See
# tests/test_cloudwatch_botocore.py.
test = ["boto3>=1.35"]

[project.entry-points."pytest11"]
substrate = "pytest_substrate.plugin"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
# E/F (pyflakes+pycodestyle), I (isort), SIM (flake8-simplify),
# BLE (blind-except), UP (pyupgrade). Keep the set explicit so CI is reproducible.
select = ["E", "F", "I", "SIM", "BLE", "UP"]
